import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() => ({

main: {
    background: 'white',
    boxShadow: '15px 10px 15px #0004',
    padding: 10,

}
}))

export default useStyles;